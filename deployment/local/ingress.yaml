apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zk-tk
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    nginx.ingress.kubernetes.io/proxy-body-size: 200m
spec:
  rules:
    - host: "zk-tk.localhost"
      http:
        paths:
          - path: "/(.*)"
            pathType: Prefix
            backend:
              service:
                name: zk-tk
                port:
                  name: api
